// Existing production fabrics; arbitrary garment fabric names remain supported.
export const PRODUCTION_FABRICS = ['deportiva', 'polar'] as const;
export const DEFAULT_PRODUCTION_FABRIC = PRODUCTION_FABRICS[0];

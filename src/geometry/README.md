# Geometría y nesting (pendiente)

Futuro límite de integración entre dominio y motor Rust. No hay algoritmo ni contrato
ejecutable todavía. Se definirá antes de implementar el motor, con geometría en mm,
siluetas reales y área de etiqueta. No usar bounding boxes como criterio final de colisión.

El optimizador debe poder reemplazarse sin modificar componentes React.
